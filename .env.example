# Public runtime config. Only EXPO_PUBLIC_* vars are bundled into the client.
# Copy to .env.local (gitignored) and adjust. Never put secrets here.

EXPO_PUBLIC_APP_ENV=development
EXPO_PUBLIC_API_BASE_URL=
EXPO_PUBLIC_SYNC_ENABLED=false
